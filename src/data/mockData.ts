import type { ModelProfile, ScanStep } from '../types';

// Optional design-time placeholders kept only for non-runtime references.
export const defaultScanSteps: ScanStep[] = [];
export const modelProfiles: ModelProfile[] = [];
